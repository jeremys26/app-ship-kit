# Agent routing (paste into AGENTS.md or CLAUDE.md)

```markdown
## App Ship Kit

For feature work, UI, security, review, and release, follow the **app-ship-kit**
skill (`.cursor/skills/app-ship-kit` or project skills). Pick one pipeline
(A–E); read only the referenced files. Do not load every reference.

If DESIGN.md is missing, offer the kit's design-brief template before UI work.
Never put secrets in `EXPO_PUBLIC_` / `NEXT_PUBLIC_` env vars.
```
